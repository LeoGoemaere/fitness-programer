export default defineAppConfig({
  ui: {
    primary: 'sky',
    // Below the default sizes for components
    button: {
      default: {
        size: 'lg'
      }
    },
    input: {
      default: {
        size: 'lg'
      }
    },
    select: {
      default: {
        size: 'lg'
      }
    },
    buttonGroup: {
      default: {
        size: 'lg'
      }
    },
  },
})